# 前端视觉与交互一致性专项 · 2026-09-25

## 目标与本轮完成条件

以现有产品为基础，先核对规范与实际页面，再修复有证据的跨页面差异。
本轮完成条件：规范入口能指导当前风格；已确认的单集入口和手机队列变更可操作；
搜索键盘问题修复；公共视觉规则降低噪声；类型、Lint、受影响回归和构建通过；
桌面、390px、320px 有实际页面证据。全量前端检查作为公共样式变更的回归补充。

基线：本地 `main@509c291`。在 `codex/frontend-consistency` 隔离工作树实施。
数据：独立临时目录的 Fixture journey，schema 36，锚点 2026-09-25 23:00 +08:00。
未使用生产数据；实现阶段未触发真实导入、同步、加工或外部写入。本记录聚焦本地实现与 Fixture 验收，PR CI、合并和生产发布以关联 GitHub 记录为准。

## 规范 review

| 发现 | 依据 | 处置 |
| --- | --- | --- |
| 总规范仍要求浅灰、紫蓝渐变、大圆角与大留白；页面已是暖纸编辑风格 | 原 DESIGN_SYSTEM.md 与首页、节目、工作流、标签、清单截图 | 重写总规范，保留排版规范为唯一数值入口 |
| 旧页面完成勾选表已经不能代表当前进度，且“每页提交一次”与当前授权合同冲突 | 原 DESIGN_SYSTEM.md 末尾 | 删除旧完成声明及隐含 Git 授权，改为证据验收入口 |
| “禁止 spin/pulse”把功能性加载与装饰性动画混为一谈 | 规范与现有加载组件 | 改为允许必要状态反馈、尊重 reduced motion |
| 排版角色较完整，可保留；字体加载说明不完整 | 根布局静态导入 CSS，DeferredEditorialAssets 延迟启用字体 | 补充首屏系统字体、就绪切换与失败回退；未改字体机制 |
| 缺少同类操作、模态键盘与手机队列合同 | 搜索、单集卡片、Inbox 实际操作 | 加入本轮确认的具体交互要求 |
| 页面级硬编码与覆盖较多，样式来源难判断 | globals.css 基线 9282 行，多处 wf-editorial、collection-* 和 rhythm-* | 提供最小公共表面 Token；本轮改动在已有规则原位修订，未进行全量 CSS 架构重写 |

## 用户已确认的决定

1. 保留并精修纸感风格，降低纹理、粗边框和重复装饰，统一密度。
2. 单集标题统一进入站内单集工作台；外链明确为原节目，简介为次要操作。
3. 手机队列改成顶部切换、单栏浏览，保留各栏位置与数量。

这些是可逆的界面取舍，不新增领域实体，不改 CONTEXT.md，不额外建立 ADR。
Inbox、Focus、Someday、Done、完成事实及加工运行继续使用原领域定义。

## 问题与首批修复

| 编号 / 优先级 | 具体问题与影响 | 本轮变更 / 验收 |
| --- | --- | --- |
| U1 / 高 | Discovery / 搜索标题进入站内，节目页单集标题去站外；同一张卡片还有工作台、阅读简介、查看详情，用户难预测目的地 | 节目卡片和精选报告标题复用 EpisodeLink 进入站内并记录来源；报告保留独立展开按钮；移除重复工作台链接；外部链接改为明确的原节目；保留简介按需读取和音视频独立入口 |
| U2 / 高 | 搜索关闭按钮 Shift+Tab 会触发失焦自动关闭，与其他模态行为不同 | 去掉失焦即关闭；只在模态搜索内循环 Tab / Shift+Tab；关闭恢复触发焦点与 body 滚动；独立搜索页不锁滚动或圈定焦点 |
| U3 / 高 | 390px Inbox 横向四栏仅露一栏及下一栏边缘，其他队列入口不可见 | 767px 及以下显示四个队列按钮和当前数量，单栏；按 queue URL 定位；切换保留已挂载队列的滚动位置；桌面并排布局及拖放保留 |
| U4 / 中 | 手机隐藏顶部导航后依然留有 64px 桌面占位，Inbox 顶部出现无效空白 | PageLayout 复用响应式导航占位变量，与 768px 导航断点一致 |
| V1 / 中 | 公共页面纸纹、清单弹窗硬阴影与紧凑工作流风格差异明显 | 纸纹叠加浅纸遮罩降低对比，Inbox 使用干净纸底；公共弹窗减弱边框与阴影、收紧内边距；首屏底色同步 |
| V2 / 中 | 清单主按钮黑底且 40px，标签/工作流主按钮橙色；清单弹窗取消与提交贴在左侧 | 清单主按钮复用公共橙色 Token，目标升到 44px；导入/刷新弹窗动作靠右并保留 8px 间距；焦点色统一 |

已保留的良好行为：大部分关键操作已有 44px 命中区域；单集工作台可返回原条目；
Show Notes 按需加载并保留错误时预览；多个现有弹窗已支持 Escape、焦点循环和触发点恢复。

## 审查步骤与覆盖

| 步骤 | 页面/状态 | 首轮观察 |
| --- | --- | --- |
| 01 | Discovery 桌面 | 阅读风格完整，纹理与多重分隔较强；报告卡片存在短内容时较大留白，需结合真实报告长度判断 |
| 02 | Inbox 桌面 | 四栏适合比较，完成与移动操作清楚；保留拖放路径 |
| 03–04 | 单集工作台桌面/手机 | 内容页签、菜单 Escape 和关闭恢复可用；转写、人物和外部加工未做真实验收 |
| 05 | Inbox 手机 | 横向栏切换困难，已按 U3 调整 |
| 06 | Discovery 手机 | 报告默认收起、最近更新优先，未擅自改阅读优先级 |
| 07 | 播客列表手机 | 列表可读、长内容未使页面横向溢出；标签本身允许横向筛选 |
| 08–09 | 节目详情与单集卡片手机 | 发现 U1；现有节目简介、标签、备注仍可用 |
| 10–11 | 工作流列表与创建第一步桌面 | 已有紧凑行与四步向导；固定弹窗高度可能造成空白，但可以避免步骤跳动，本轮保留 |
| 12 | 搜索结果桌面与键盘 | 确认 U2；日期格式与其他页面存在不同，作为后续文本规范清理项 |
| 13 | 标签管理桌面 | 页面可用，密度合理；没有把少量 Fixture 数据产生的空白判为业务缺陷 |
| 14 | 导入首页桌面 | 初始状态清楚；未选择文件、运行同步或实际导入 |
| 15–16 | 清单空态与导入弹窗桌面 | 发现 V2；只检查空态和输入入口，未向源站发起导入 |

截图和逐步说明随本次交付纳入 `output/reviews/frontend-consistency-2026-09-25/`。
截图是本轮 Fixture 页面证据，封面黑块/占位图来自测试数据，不作为真实内容质量结论。

## 后续专项覆盖

以下是明确的覆盖缺口和下一批候选，不写成已修复或已验收：

- 高优先级：转写、人物与发言、单集助手之间的长流程；异步失败、重试、保存与返回，使用获准且适用的数据。
- 高优先级：工作流第二至四步、执行详情、报告，以及导入预览/运行/重试；保持用户数据和任务语义。
- 中优先级：Discovery / Curated Lists / 播客清单命名与日期格式；需结合现有词汇和真实使用语境决定统一范围。
- 中优先级：检查完整明暗模式、对比度、屏幕阅读器和系统缩放；截图不能证明 WCAG 全面合规。
- 中优先级：在触及页面时逐步收敛已有覆盖样式，避免另造平行组件库或一次性重写全站 CSS。

## 验证记录

见文末当前运行结果；代码、自动化测试、Fixture 实机、生产证据分开记录。

- 自动化：152 个测试文件、1213 项全量前端测试通过。全量后增加报告标题入口对齐，52 项报告定向测试通过；搜索/Inbox 最后清理后的 63 项定向测试通过。早期失败是删去旧标题外链后残留的引用、旧行为/色值断言和测试环境隐藏导航的取值方式，均已修复并复验。
- 最终 TypeScript 与 ESLint 均通过；最后窄屏 CSS 调整后的 Next.js 构建通过。
- 本地构建预览使用显式 `BACKEND_URL=http://127.0.0.1:18080`，前端端口 3001；`/ready` 回读 fixture / journey / schema 36 / ready OK。最初构建未携带该环境变量、代理仍指向 8080 的预览问题已通过重新构建修复。
- 实机：1440px 四栏；767px、390px、320px 单栏，四个视口及 768px 均无页面横向溢出。Focus → Someday → Focus 保留 266.5px 滚动位置；关闭单集恢复原 Focus 条目与 queue URL。
- 节目页与报告标题均实际进入对应 `/episodes/:id` 工作台；关闭返回原入口。报告箭头独立展开/收起，长标题 320px 时不受并排按钮挤压。
- 搜索 Shift+Tab 从关闭按钮回到末端筛选，Tab 再回关闭按钮；Escape 恢复搜索触发按钮、来源 URL 与页面滚动。
- 清单弹窗 320px 下输入框、关闭、取消、预览均为 44px；检查了按钮对齐和无横向溢出。
- 文档相对链接和 `git diff --check` 通过；本次交付包含本轮源码、相邻回归测试、设计文档和截图证据包。Next.js 自动生成文件已还原/隔离；原项目工作区保持干净。
- 本记录不替代 GitHub CI、PR、部署或生产运行证据。未做真实数据库写入、外部加工、真实导入或完整屏幕阅读器验收。桌面拖放业务回归由既有自动化覆盖，本轮未执行真实浏览器拖放操作。

审查报告与截图包位于 `output/reviews/frontend-consistency-2026-09-25/index.html`，
含 16 张基线截图、首批变更对照及响应式测量。运行预览是本机临时服务，不是生产发布。
