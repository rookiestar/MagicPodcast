# Inbox 最近完成验收

日期：2026-08-23
关联：[GitHub #169](https://github.com/rookiestar/MagicPodcast/issues/169)

## 1. 专项信息

| 字段 | 内容 |
| --- | --- |
| 用户动作 | 打开 Inbox、明确完成、撤销或重新处理 |
| 页面 / 接口 | `/inbox`、`/api/v1/consumption/queues/done` |
| 分支 | `codex/inbox-done-history-spec` |
| 目标环境 | 本地生产构建 + Fixture `complete-v2` |
| 人审状态 | 已确认（#168 Spec） |
| 数据库或部署授权 | 真实数据库迁移与生产部署均未授权、未执行 |

## 2. 用户体验不变量

> 最近完成变慢或失败时，Inbox、Focus、Someday 仍先显示并保持可操作；首次访问在最近完成请求结束前不显示虚假空态。

## 3. 测量边界

- 前端：`next start`，`127.0.0.1:3000`。
- 后端：本地 Data Profile，schema 19，Fixture `complete-v2-journey-20260823T20-schema-19`。
- 浏览器：Playwright Chromium；冷态为全新浏览器会话，热态为同一会话刷新。
- 视口：桌面 `1440×1000`、iPad 横屏 `1180×820`、iPad 竖屏 `820×1180`、手机 `390×844`。
- 采样：冷态 1 次、热态 3 次；慢请求和失败请求各 1 次受控注入。

本记录证明本地代码与用户可见行为，不代表生产运行结果。

## 4. 状态验收矩阵

| 场景 | 输入 / 注入方式 | 实际用户结果 |
| --- | --- | --- |
| 正常请求 | Fixture 正常响应 | 四栏内容与顺序正确；最近完成显示实际 2 条、完成时间和独立操作入口 |
| 慢请求 | 最近完成接口延迟 1200ms | 行动队列 107ms 可用，最近完成 1400ms 出现；等待期间未显示虚假空态 |
| 请求失败 | 最近完成接口受控返回 500 | 三条行动队列保持可见；仅最近完成显示局部错误；解除注入后单独重试恢复 |
| 首次访问 | 全新浏览器会话 + 慢请求 | 保持加载骨架，无空白页、无提前空态 |
| 缓存回访 | 同一会话刷新 | 本功能未新增缓存；每次独立请求，行动队列与最近完成互不阻塞 |
| 分页 / 滚动返回 | 不适用 | #169 最近完成固定最多 20 条；分页属于 #170 |

## 5. 响应与有效内容时间

| 场景 | 有效内容出现时间 | 最近完成 API | 说明 |
| --- | ---: | ---: | --- |
| 冷态 | 150ms | 3.0ms | 首个最近完成卡片可见；DOMContentLoaded 38.4ms |
| 热态 1 | 89ms | 2.8ms | 同一浏览器会话刷新 |
| 热态 2 | 86ms | 2.4ms | 同一浏览器会话刷新 |
| 热态 3 | 90ms | 5.2ms | 同一浏览器会话刷新 |
| 受控慢请求 | 行动队列 107ms；最近完成 1400ms | 注入 1200ms 延迟 | 空态暴露率 0/1 |

最近完成响应的浏览器编码传输量为 1063 bytes。样本仅用于本票本地回归，不与历史生产基线直接比较。

## 6. 用户可见交互

| 视口 / 交互 | 结果 |
| --- | --- |
| 桌面 | 四栏、完成时间、15 秒倒计时、即时撤销、超时后重新处理通过 |
| 桌面拖放 | 从 Focus 拖入最近完成通过；从最近完成拖回 Inbox 并精确放到首项前通过 |
| iPad 横屏 | 11 个拖动入口可用；页面无横向溢出 |
| iPad 竖屏 | 拖动入口隐藏；11 个移动菜单和 9 个完成按钮可用；页面无横向溢出 |
| 手机 | 与竖屏相同的菜单/按钮回退可用；移动导航正常；页面无横向溢出 |
| 键盘 | 移动菜单可聚焦，以 Enter 打开、方向键移动、Escape 关闭 |

本地截图和 Playwright 快照保存在未提交的 `output/playwright/issue-169/`。

## 7. 自动化证据

| 检查 | 结果 |
| --- | --- |
| `cd backend && go test ./...` | 通过 |
| `cd backend && go vet ./...` | 通过 |
| `cd frontend && npm run type-check` | 通过 |
| `cd frontend && npm run lint` | 通过 |
| `cd frontend && npm run test:run` | 105 个文件、602 项测试通过 |
| `cd frontend && npm run build` | 通过 |
| Fixture status / `/ready` / 最近完成 API | schema 19、Fixture `complete-v2`、ready；单集 2012 可见 |

## 8. 收口判断

- [x] 正常、慢、失败、首次访问均已覆盖。
- [x] API 响应与有效内容时间均已记录。
- [x] 慢或失败时三条行动队列保持可用。
- [x] 桌面、iPad 横/竖屏、手机和键盘路径已验收。
- [x] 未执行的真实数据库迁移与生产部署未写成已完成。

本地代码与验收证据满足 #169；生产迁移、部署和生产验收仍需另行授权。
