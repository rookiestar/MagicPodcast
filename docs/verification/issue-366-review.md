# #366 交付审查

审查对象：`worktree-issue-366-people-bar` 在 `29a4a322c3de7d0607bd1086d7654d85e44bf3fe` 上的三个文件，以及 output/issue366 中既有截图。审查开始时 GitHub 未发现关联 PR。修复副本位于独立分支 `codex/issue366-review`，原交付工作树未改动。

## 确认并修复的问题

- P2：提示通过 350ms 时间阈值跳过 focus 后的 click；触屏按住超过阈值再松开时，刚展开的说明会立即关闭，键盘聚焦后立即激活也会被忽略。改为记录 pointerdown 前的展开状态，点击据此切换，键盘激活不依赖时钟；指针取消和失焦清理记录。
- P2：识别完成时提示节点消失，但展开状态残留；首次 Escape 被不可见提示吞掉，无法正常关闭草稿核对弹层。任务结束和主动收起时清理提示状态。

## 验证

新增回归在原交付实现上运行：2 项失败，分别复现上述问题；修复后相关组件测试 26 项通过。TypeScript、定向 ESLint 与 diff 空白检查通过。

检查了既有桌面底栏、390px 展开提示及窄屏放大截图，布局符合精简方向；放大截图中的开发工具浮标遮挡了提示，不能作为触屏热区未遮挡的充分证据。本轮没有新增真实浏览器操作，触屏及键盘修复的浏览器复验仍待完成，不能用组件测试替代。

审查未发现需要改变 SSE、取消协议、草稿持久化或确认生效语义的问题。审查阶段未执行生产部署或真实数据修改。
