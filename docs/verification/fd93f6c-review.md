# fd93f6c 审查与修复

范围：`fd93f6c^..fd93f6c`，提交实际为 #371 的证据核对交付，同时遵循 #355 的草稿核对、明确应用及 Copilot 一致性要求。修复位于 `codex/fd93f6c-review`，不修改原交付工作树。

## 发现

- P1，正确性：同一候选跨多个 Speaker，统一更正姓名后分批确认，会创建不同人物 ID，导致可靠发言与 Copilot 语料分裂。原实现测试复现 ID 1 与 ID 2 不相同。应用时从当前草稿已经确认的身份初始化候选映射，保留跨请求一致性；候选 ID 加姓名是本草稿内键，不扩张跨集身份规则。另以不同候选同名的分批确认测试确保不合并。
- P2，需求符合性：原始直接建议被换成手填或更正姓名后，仍显示“直接证据”，将原证据误用于新决定。改为“人工选择 · 待确认”和“查看原始建议依据”；不改变原始证据或确认协议。

## 验证范围

后端通过临时测试数据库验证分批应用后的身份 ID、可靠发言数量及同名隔离；前端通过既有完整人物核对组件测试验证改名、手填及提示语义，未自动调用应用接口。既有服务、Handlers、Copilot 测试覆盖草稿、来源及取消链路。

通过：人物服务、Handlers、Copilot 三包测试及 race、go vet；前端人物组件 31 项测试、TypeScript、改动文件 ESLint；`git diff --check`。

审查验证未运行真实模型、真实浏览器或生产服务；不将组件测试视为实际模型识别质量或浏览器验收。未执行生产部署或历史已分裂人物回填。
