# #398 交付复核与修复

审查对象：PR #407，合并提交 `b683a227cbb5e23052545a6f0a6f71c10b2a492b` 相对其父提交。参考 glm 侧栏终端总结、父 Spec 与实施票，沿导入页面、双 API、任务、身份、元数据、单集同步及缓存调用链复核。以下均为本轮确认并修复的问题。

修复工作树：`codex/issue-398-review-fixes`。主工作区已有改动未动；修复尚未提交。

## 发现（按重要性）

| 等级 | 问题与影响 | 修复位置 |
|---|---|---|
| P1 | 同文件不同 RSS 地址的同身份节目并发完成抓取时，都可能先查到“无候选”并各自插入；已删除记录也未参与抓取后的身份核对。产生重复节目或绕过删除边界。 | `backend/internal/sync/opml_import.go`、`podcast_identity.go`：网络保持并发，身份核对与写入互斥；纳入已删除候选；禁止已有 URL 与另一候选同时存在时盲目改绑。恢复与资料写入合为单次数据库更新。 |
| P1 | 进行中只保存计数，逐项结果最后才保存；summary 先发送，建任务/终态落库失败还继续或报告成功。重启丢失完成范围，浏览器可能收到假成功。 | `backend/internal/sync/import_task.go`、`handlers/sync.go`：先保存输入范围、逐项保存结果与分类统计，终态成功保存后才发汇总；持久化失败不再静默降级；修正注册/终态查询竞态。 |
| P2 | SSE 首响应后清除计时器，后续 reader.read 可无限等待；写失败后仍保留 ticker 直到整个导入结束。 | `frontend/src/lib/sseClient.ts`、`backend/internal/handlers/sse.go`：预算覆盖流读取、正确区分超时；写失败立即幂等释放保活资源。 |
| P2 | 命中 PodcastIndex 后在线抓取只取集数等四项，继续用旧索引标题/简介/作者；重导缺字段清空资料，优先级和更新频率被默认值覆盖；网站没有从 RSS 映射，单独改网站也检测不到。 | `backend/internal/sync/opml_import.go`、`podcast_helpers.go`、`metadata_sync_plan.go`：最新源站字段优先，缺字段保留，保留个人同步设置，补齐网站映射和变化检测。 |
| P2 | 更新已有节目为待同步时提前 return，未失效缓存；绑定地址也未失效替代源缓存。列表可能继续展示旧状态。 | `backend/internal/sync/podcast_helpers.go`：成功写入后定向失效。 |
| P2 | 首次“自上次更新”无单集游标仍默认最近七天，会跳过历史内容；空结果或单窗处理期间取消可能仍推进游标。 | `backend/internal/sync/episode_sync.go`：无游标采用历史起点，处理前/逐项/提交游标前检查取消。 |
| P2 | 页面丢弃任务 entries，没有抓取后身份冲突的确认入口；快速换文件可能显示旧预览；断线查询 latest 而非已收到的任务 ID；重试未纳入前端操作互斥；恢复结果只记一条 success 日志，统计把日志数当节目数。 | `frontend/src/hooks/useImportSyncOperations.ts`、`ImportOpmlPanel.tsx`：完整结果面板、显式关联确认、预览序号、按任务 ID 恢复、受控重试/未完成条目继续、从持久结果生成汇总；补齐 SSE 分类字段。 |

## 验证

- 后端 `go test ./...`、`go vet ./...` 通过；后续改动对 sync/handlers 重跑测试与 vet 通过。
- 前端全量 145 文件、1068 测试通过；后续新增/改动涉及的 3 文件、16 测试再次通过。TypeScript、ESLint、生产构建通过。
- 新回归覆盖：同身份并发地址不重复、已删除身份不绕过确认、实时元数据优先、缺字段及个人设置保留、终态写失败不发成功、缺任务表时拒绝写订阅、逐项范围先持久化、首条 SSE 后停流超时、页面逐项冲突确认、轮询恢复统计。
- Playwright 真实页面 → 实际导入 API → glm 隔离测试库的独立副本：导入三个测试条目，页面刷新后仍为任务 #5，保留进行中的逐项范围，最终恢复为新增 1、待同步 1、失败 1。慢 RSS 超过单次抓取预算，正确保留待同步。
- 截图：`/tmp/mp398-review/disconnected-running.png`、`/tmp/mp398-review/completed.png`。后者还暴露了日志统计错误，随后修复，并通过恢复统计定向测试；截图不冒充最终统计修复后的页面证据。
- 日志位于 `/tmp/mp398-review-*`。本轮启动的临时服务已停止。`git diff --check` 通过。

## 边界

- 尚未 commit/push/新建 PR；未部署、未对真实库迁移或导入真实 OPML。原票关闭和原 CI 不能替代本轮修复交付。
- 进程崩溃若发生在节目写入之后、逐项结果落库之前，该项仍标为“尚无已完成结果”，不声称没有写入；显式继续时重新核对本地记录。没有加入自动全量重跑。
- 当前最小任务表仍以 JSON 保存逐项结果，本轮未做超大 OPML 压力测试；原有 8 MiB 上传边界测试通过。
